namespace Backend_PcAuction.Utils
{
    public class PurchaseStatuses
    {
        public const string PendingPayment = nameof(PendingPayment);
        public const string Paid = nameof(Paid);
        public const string Terminated = nameof(Terminated);
    }
}
